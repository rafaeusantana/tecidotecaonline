<%= form_for(@tipo_bandeira, :url => { :controller => "tipo_bandeiras"}) do |f| %>
  <% if @tipo_bandeira.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_bandeira.errors.count, "error") %> prohibited this tipo_bandeira from being saved:</h2>

      <ul>
      <% @tipo_bandeira.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <input type="hidden" id="id" name="id" value="<%= @tipo_bandeira.try(:id) %>" />

<div style="width:50%">
<div class="panel panel-info">
  <div class="panel-heading">
    <h3 class="panel-title">Descrição</h3>
  </div>
  <div class="panel-body">
	  <div class="form-group">
	    <%= f.label :tipo %> *
	    <%= f.text_field :tipo, :class=>"form-control input-sm" %>
	  </div>
  </div>
</div>
</div>
  <div class="actions">
    <%= f.submit :class=>"btn btn-primary", :value=>"Inserir" %>
  </div>
<% end %>
