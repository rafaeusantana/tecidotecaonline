<%= form_for(@tipo_item_variado, :url => { :controller => "tipo_item_variados"}) do |f| %>
  <% if @tipo_item_variado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_item_variado.errors.count, "error") %> prohibited this tipo_item_variado from being saved:</h2>

      <ul>
      <% @tipo_item_variado.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <input type="hidden" id="id" name="id" value="<%= @tipo_item_variado.try(:id) %>" />

<div style="width:50%">
<div class="panel panel-info">
  <div class="panel-heading">
    <h3 class="panel-title">Descrição</h3>
  </div>
  <div class="panel-body">
	  <div class="form-group">
	    <%= f.label :tipo %> *
	    <%= f.text_field :tipo, :class=>"form-control input-sm" %>
	  </div>
  </div>
</div>
</div>
  <div class="actions">
    <%= f.submit :class=>"btn btn-primary", :value=>"Inserir" %>
  </div>
<% end %>
