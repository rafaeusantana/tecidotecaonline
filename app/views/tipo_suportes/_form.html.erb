<%= form_for(@tipo_suporte, :url => { :controller => "tipo_suportes"}) do |f| %>
  <% if @tipo_suporte.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_suporte.errors.count, "error") %> prohibited this tipo_suporte from being saved:</h2>

      <ul>
      <% @tipo_suporte.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <input type="hidden" id="id" name="id" value="<%= @tipo_suporte.try(:id) %>" />

<div style="width:50%">
<div class="panel panel-info">
  <div class="panel-heading">
    <h3 class="panel-title">Descrição</h3>
  </div>
  <div class="panel-body">
	  <div class="form-group">
	    <%= f.label :nome %> *
	    <%= f.text_field :nome, :class=>"form-control input-sm" %>
	  </div>
  </div>
</div>
</div>
  <div class="actions">
    <%= f.submit :class=>"btn btn-primary", :value=>(@tipo_suporte.id.nil?) ? "Adicionar" : "Atualizar" %>
  </div>
<% end %>
