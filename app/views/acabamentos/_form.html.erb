<%= form_for(@acabamento, :url => { :controller => "acabamentos"}) do |f| %>
  <% if @acabamento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@acabamento.errors.count, "error") %> prohibited this acabamento from being saved:</h2>

      <ul>
      <% @acabamento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <input type="hidden" id="id" name="id" value="<%= @acabamento.try(:id) %>" />

<div style="width:50%">
<div class="panel panel-info">
  <div class="panel-heading">
    <h3 class="panel-title">Descrição</h3>
  </div>
  <div class="panel-body">
	  <div class="form-group">
	    <%= f.label :tipo %> *
	    <%= f.text_field :tipo, :class=>"form-control input-sm" %>
	  </div>
  </div>
</div>
</div>
  <div class="actions">
    <%= f.submit :class=>"btn btn-primary", :value=>(@acabamento.id.nil?) ? "Adicionar" : "Atualizar" %>
  </div>
<% end %>
