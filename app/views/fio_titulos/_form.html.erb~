<%= form_for(@fio_titulo, :url => { :controller => "fio_titulos"}) do |f| %>
  <% if @fio_titulo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fio_titulo.errors.count, "error") %> prohibited this fio_titulo from being saved:</h2>

      <ul>
      <% @fio_titulo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <input type="hidden" id="id" name="id" value="<%= @fio_titulo.try(:id) %>" />

<div style="width:50%">
<div class="panel panel-info">
  <div class="panel-heading">
    <h3 class="panel-title">Descrição</h3>
  </div>
  <div class="panel-body">
	  <div class="form-group">
	    <%= f.label :tipo %> *
	    <%= f.text_field :nome, :class=>"form-control input-sm" %>
	  </div>
  </div>
</div>
</div>
  <div class="actions">
    <%= f.submit :class=>"btn btn-primary", :value=>"Inserir" %>
  </div>
<% end %>
