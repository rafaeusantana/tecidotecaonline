<%= form_for(@tecnica_design, :url => { :controller => "tecnica_designs"}) do |f| %>
  <% if @tecnica_design.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tecnica_design.errors.count, "error") %> prohibited this tecnica_design from being saved:</h2>

      <ul>
      <% @tecnica_design.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <input type="hidden" id="id" name="id" value="<%= @tecnica_design.try(:id) %>" />

<div style="width:50%">
<div class="panel panel-info">
  <div class="panel-heading">
    <h3 class="panel-title">Descrição</h3>
  </div>
  <div class="panel-body">
	  <div class="form-group">
	    <%= f.label :nome %> *
	    <%= f.text_field :nome, :class=>"form-control input-sm" %>
	  </div>
  </div>
</div>
</div>
  <div class="actions">
    <%= f.submit :class=>"btn btn-primary", :value=>(@tecnica_design.id.nil?) ? "Adicionar" : "Atualizar" %>
  </div>
<% end %>
