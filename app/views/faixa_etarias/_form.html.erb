<%= form_for(@faixa_etaria, :url => { :controller => "faixa_etarias"}) do |f| %>
  <% if @faixa_etaria.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@faixa_etaria.errors.count, "error") %> prohibited this faixa_etaria from being saved:</h2>

      <ul>
      <% @faixa_etaria.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <input type="hidden" id="id" name="id" value="<%= @faixa_etaria.try(:id) %>" />

<div style="width:50%">
<div class="panel panel-info">
  <div class="panel-heading">
    <h3 class="panel-title">Descrição</h3>
  </div>
  <div class="panel-body">
	  <div class="form-group">
	    <%= f.label :nome %> *
	    <%= f.text_field :nome, :class=>"form-control input-sm" %>
	  </div>
  </div>
</div>
</div>
  <div class="actions">
    <%= f.submit :class=>"btn btn-primary", :value=>(@faixa_etaria.id.nil?) ? "Adicionar" : "Atualizar" %>
  </div>
<% end %>
