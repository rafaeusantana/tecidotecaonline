<%= form_for(@padronagem_design, :url => { :controller => "padronagem_designs"}) do |f| %>
  <% if @padronagem_design.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@padronagem_design.errors.count, "error") %> prohibited this padronagem_design from being saved:</h2>

      <ul>
      <% @padronagem_design.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <input type="hidden" id="id" name="id" value="<%= @padronagem_design.try(:id) %>" />

<div style="width:50%">
<div class="panel panel-info">
  <div class="panel-heading">
    <h3 class="panel-title">Descrição</h3>
  </div>
  <div class="panel-body">
	  <div class="form-group">
	    <%= f.label :tipo %> *
	    <%= f.text_field :tipo, :class=>"form-control input-sm" %>
	  </div>
  </div>
</div>
</div>
  <div class="actions">
    <%= f.submit :class=>"btn btn-primary", :value=>(@padronagem_design.id.nil?) ? "Adicionar" : "Atualizar" %>
  </div>
<% end %>
