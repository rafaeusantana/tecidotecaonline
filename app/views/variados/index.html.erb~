<h1>Lista de Itens Variados</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th>Localização</th>
      <th>Tipo da Aquisição</th>
      <th>Data de Aquisição</th>
      <th>Fabricante</th>
      <th colspan="8"></th>
    </tr>
  </thead>

  <tbody>
    <% @outros.each do |outro| %>
      <tr>
        <td><%= outro.id %></td>
        <td><%= outro.localizacao %></td>
        <td><%= outro.tipo_aquisicao %></td>
        <td><%= outro.data_aquisicao %></td>
        <td><%= outro.fabricante.nome %></td>
        <td><%= link_to 'Mostrar', outro_path(outro) %></td>
        <td><%= link_to 'Editar', edit_outro_path(outro) %></td>
        <td><%= link_to 'Excluir', outro_path(outro), method: :delete, data: { confirm: 'Você tem certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Adicionar item', new_outro_path %>
